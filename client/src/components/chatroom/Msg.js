import React from 'react'
import Message from './Message'

const Msg = ({date, messsages, currentUser}) => {
  console.log(date, messsages, currentUser)
  
  messsages.for

  return (
    <div>
      
    </div>
  )
}

export default Msg
